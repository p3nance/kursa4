<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controllers.AuthController"
            stylesheets="/views/styles/cabinet-auth.css"
            styleClass="auth-bg">

    <StackPane AnchorPane.topAnchor="0" AnchorPane.bottomAnchor="0" AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0">
        <HBox alignment="CENTER" spacing="0" style="-fx-pref-width: 1200;">
            <!-- Левая часть: логотип по центру ОСНОВНОЙ формы -->
            <VBox alignment="CENTER" spacing="22" style="-fx-background-color: transparent; -fx-pref-width: 520;">
                <Label text="💻" styleClass="left-logo-label"/>
                <Label text="TechStore" styleClass="left-title-label"/>
                <Label text="Технологии, доступные каждому! Быстро. Удобно. Надежно."
                       styleClass="left-slogan-label"
                       wrapText="true"
                       maxWidth="350"/>
            </VBox>
            <!-- Правая часть: форма по центру стек-карточки -->
            <VBox alignment="CENTER" spacing="0" styleClass="auth-card" prefWidth="410" prefHeight="520">
                <Label text="Добро пожаловать!" styleClass="auth-title" />
                <Label fx:id="authSubTitle"
                       styleClass="auth-label"
                       text="Пожалуйста, авторизуйтесь или создайте аккаунт"
                       wrapText="true"
                       maxWidth="340"/>
                <VBox spacing="13" prefWidth="340" style="-fx-padding: 22 0 15 0;">
                    <Label text="Email:" styleClass="auth-label"/>
                    <TextField fx:id="authEmail" promptText="Введите email" styleClass="auth-field"/>
                    <Label text="Пароль:" styleClass="auth-label"/>
                    <PasswordField fx:id="authPassword" promptText="Введите пароль" styleClass="auth-field"/>
                    <PasswordField fx:id="authConfirmPassword" visible="false" promptText="Подтвердите пароль" styleClass="auth-field"/>
                </VBox>
                <HBox alignment="CENTER" spacing="16" style="-fx-padding: 11 0 13 0;">
                    <Button fx:id="authSubmitBtn" text="Вход" styleClass="auth-submit-btn"/>
                    <Button fx:id="authSwitchBtn" text="Регистрация" styleClass="auth-switch-btn"/>
                </HBox>
                <Button fx:id="authBackBtn" text="← Назад" styleClass="auth-back-btn"/>
                <Label fx:id="authErrorLabel" style="-fx-text-fill: #ef4444; -fx-font-size: 14px;" visible="false"/>
            </VBox>
        </HBox>
    </StackPane>
</AnchorPane>
