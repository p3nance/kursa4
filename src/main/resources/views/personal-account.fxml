<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="controllers.PersonalAccountController"
            stylesheets="@styles/styles.css">

    <top>
        <VBox spacing="10" style="-fx-padding: 15;">
            <Label text="👤 Личный кабинет" styleClass="title"/>
        </VBox>
    </top>

    <center>
        <ScrollPane fitToWidth="true" style="-fx-padding: 0;">
            <VBox spacing="20" style="-fx-padding: 20;">

                <!-- Профиль -->
                <VBox spacing="10" styleClass="card">
                    <Label text="Профиль" styleClass="subtitle"/>
                    <GridPane hgap="10" vgap="10">
                        <columnConstraints>
                            <ColumnConstraints prefWidth="150"/>
                            <ColumnConstraints hgrow="ALWAYS"/>
                        </columnConstraints>

                        <Label text="Email:" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
                        <TextField fx:id="emailField" editable="false" GridPane.columnIndex="1" GridPane.rowIndex="0"/>

                        <Label text="Имя:" GridPane.columnIndex="0" GridPane.rowIndex="1"/>
                        <TextField fx:id="nameField" GridPane.columnIndex="1" GridPane.rowIndex="1"/>

                        <Label text="Телефон:" GridPane.columnIndex="0" GridPane.rowIndex="2"/>
                        <TextField fx:id="phoneField" GridPane.columnIndex="1" GridPane.rowIndex="2"/>

                        <Label text="О себе:" GridPane.columnIndex="0" GridPane.rowIndex="3"/>
                        <TextArea fx:id="bioArea" GridPane.columnIndex="1" GridPane.rowIndex="3" prefRowCount="3"/>
                    </GridPane>
                    <Button fx:id="saveButton" text="💾 Сохранить" styleClass="button-success"/>
                </VBox>

                <!-- История заказов -->
                <VBox spacing="10" styleClass="card">
                    <Label text="📦 История заказов" styleClass="subtitle"/>
                    <TableView fx:id="ordersTable" prefHeight="150">
                        <columns>
                            <TableColumn text="Номер" prefWidth="100"/>
                            <TableColumn text="Дата" prefWidth="120"/>
                            <TableColumn text="Сумма" prefWidth="100"/>
                            <TableColumn text="Статус" prefWidth="100"/>
                        </columns>
                    </TableView>
                </VBox>

                <!-- Адреса доставки -->
                <VBox spacing="10" styleClass="card">
                    <Label text="🏠 Адреса доставки" styleClass="subtitle"/>
                    <HBox spacing="10">
                        <TextField fx:id="addressField" promptText="Введите адрес..." HBox.hgrow="ALWAYS"/>
                        <Button fx:id="addAddressButton" text="Добавить" styleClass="button-success"/>
                    </HBox>
                    <TableView fx:id="addressesTable" prefHeight="100">
                        <columns>
                            <TableColumn text="Адрес" prefWidth="400"/>
                        </columns>
                    </TableView>
                </VBox>

                <!-- Безопасность -->
                <VBox spacing="10" styleClass="card">
                    <Label text="🔐 Безопасность" styleClass="subtitle"/>
                    <Button fx:id="changePasswordButton" text="🔑 Изменить пароль" styleClass="button"/>
                    <Button fx:id="logoutButton" text="🚪 Выход" styleClass="button-danger"/>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

</BorderPane>